#!/bin/sh
# Compatibility notice: the old renaming commands pointed at nonexistent files.
# Keep original filenames intact; regenerate optimized copies instead.
printf '%s\n' 'No originals have been renamed.' 'Run: python scripts/optimize-images.py'
